---
title: Weaver
description: Lightweight coordination and optional shared context for coding agents in the same repo.
template: splash
tableOfContents: false
---

import StatusDemo from "../../components/StatusDemo.astro";

<div class="wv-home">

Lightweight status, task intent, advisory claims, activity, and durable Repository Facts for
coding agents. Optional revision-safe Markdown scratchpads support collaborating workstreams.
Claude Code, Codex, OpenCode, Pi, and ordinary terminals share one local SQLite commons.

<StatusDemo />

```sh frame="none" wrap
curl -fsSL https://raw.githubusercontent.com/sean35mm/weaver/main/install.sh | sh
```

One self-contained binary for macOS and Linux — no Node, coordination daemon,
cloud account, or MCP server. Everything stays on your machine. Use the rich/source
scratchpad UI, a normal browser, headless URL, terminal commands, or `$EDITOR`.

<p class="wv-links">
  [Get started](/weaver/getting-started/install/) · [Scratchpads](/weaver/guides/scratchpads/) · [GitHub](https://github.com/sean35mm/weaver)
</p>

</div>

<style>{`
  /* Monotone page: black/white/gray only — the status demo is the single splash of color. */
  main h1 {
    max-width: 38rem;
    margin: clamp(2rem, 8vh, 5rem) auto 0;
    font-family: var(--sl-font-mono, ui-monospace, monospace);
    font-size: 1.6rem;
    font-weight: 600;
    text-transform: lowercase;
    letter-spacing: 0.02em;
    color: var(--sl-color-white);
  }
  .wv-home {
    max-width: 38rem;
    margin: 0 auto 18vh;
    font-size: 1rem;
    line-height: 1.7;
  }
  .wv-home a {
    color: var(--sl-color-white);
    text-decoration: underline;
    text-decoration-color: var(--sl-color-gray-5);
    text-underline-offset: 3px;
  }
  .wv-home a:hover {
    text-decoration-color: var(--sl-color-white);
  }
  .wv-home .wv-links {
    margin-top: 2.25rem;
    color: var(--sl-color-gray-4);
  }
`}</style>
